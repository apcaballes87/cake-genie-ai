# Plan: Cold Caking Page - Menu Drawer & Mobile Nav Bar

## Goal
1. Make the Cold Caking page menu drawer consistent with the Landing page drawer
2. Add mobile nav bar (MobileBottomNav) to Cold Caking page that shows/hides based on image upload state

## Files to Modify
- `src/app/coldcaking/ColdCakingClient.tsx`

## Changes Required

### 1. Menu Drawer Consistency (Landing Page → Cold Caking)
**Current**: Cold Caking has a simple drawer with basic nav links only (lines 688-725)
**Target**: Replace with the full Landing page drawer including:
- "Browse Cakes" link
- Collapsible "Shop by Occasion" accordion with categories
- All other nav links (Cold Caking, Our Bakers, Blog, How to Order, Payment Options, Delivery Rates, About Us, Contact)

**Required imports to add**:
- `ChevronDown` from lucide-react (for accordion toggle)
- `useEffect` is already imported (for category loading)

**New state variables needed**:
- `isOccasionOpen` - controls accordion expansion
- `categoriesList` - stores category data from useEffect (already exists in LandingClient)

### 2. Mobile Nav Bar (MobileBottomNav)
**Implementation**: Add `<MobileBottomNav />` component at the bottom of the page
**Conditional visibility**:
- Show when: `!hasUploadedPhoto` (no image uploaded yet)
- Hide when: `hasUploadedPhoto` (user has uploaded image, Add to Cart view shows)
- Re-show when: user removes the image (using existing `handleDeletePhoto` callback)

**Required imports**:
- `MobileBottomNav` from '@/components/MobileBottomNav'

### 3. Implementation Details

**Drawer section** (replace lines 688-725):
- Add ChevronDown import
- Add state: `const [isOccasionOpen, setIsOccasionOpen] = useState(false);`
- Add categories from useEffect (lines loading categories)
- Replace drawer content with full Landing page drawer structure including accordion

**Mobile Nav Bar**:
- Add import for MobileBottomNav
- Add conditional rendering: `{!hasUploadedPhoto && <MobileBottomNav onUploadClick={() => setIsUploaderOpen(true)} />}`
- Position: after the closing `</main>` and before the drawer (or inside main, before Footer)

## Verification
After implementation:
1. Cold Caking page should show the same drawer as Landing page
2. Mobile nav bar should appear on mobile viewport
3. Nav bar should hide when user uploads an image (shows Add to Cart)
4. Nav bar should reappear when user removes the uploaded image